# Vendor Patch Ledger

This repository treats `vendor/hermes-agent/` and `vendor/hermes-webui/` as imported upstream trees.

Policy:
- Do not edit files under `vendor/` unless the change is unavoidable.
- Prefer root-level wrappers, environment wiring, and documentation over vendored patches.
- Any vendored edit must be recorded here before it is considered acceptable.
- If a vendored file changes without a matching entry here, treat it as a bug.

## Current status

No local vendored patches are required right now.

## Ledger

| Date | File | Reason | Local patch summary | Upstream status |
| ---- | ---- | ------ | ------------------- | --------------- |
| _none_ | _none_ | _none_ | _none_ | _none_ |
